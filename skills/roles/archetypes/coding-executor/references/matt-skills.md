# Matt Pocock Skills — coding-executor

> 完整映射 → [`../_shared/mattpocock-bridge.md`](../_shared/mattpocock-bridge.md)

## 何时 invoke

| 场景 | Skill |
|------|-------|
| 开工前对齐方案 | `/grill-with-docs` |
| 加功能 / 修 bug | `/tdd` |
| 难缠 bug | `/diagnose` |
| 接手陌生代码 | `/zoom-out` |

## 安装

`npx skills@latest add mattpocock/skills` 或使用 `openclaw_space/matt-skills/` 镜像。

## Per-repo

读 `docs/agents/issue-tracker.md` 等（由 setup-matt-pocock-skills 生成）。
