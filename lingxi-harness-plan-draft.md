# Coding Agent Harness 调研 — 给灵霄的方案讨论

> 目标：解决灵霄改代码产生新 bug 的问题
> 核心理念：改前基线 → 修改 → 改后验证 → 通过合并/失败回退
> 关键约束：角色分离（写 ≠ 审），每一步边缘条件明确，harness 本身不引入 bug

---

## 一、社区已有的 Harness 项目

### 1️⃣ autonomous-coding-harness（GantisStorm）
https://github.com/GantisStorm/autonomous-coding-harness

**核心能力：**
- Unit Test Creation — 新代码自动生成测试（支持 pytest / Jest / Vitest / Go / Rust）
- Test Repair Loop — 测试失败自动修复
- Regression Testing — 改前改后自动对比

### 2️⃣ Cadence（axledbetter）
https://github.com/axledbetter/cadence

**最对味的一个。核心设计：**
- **多模型角色分离** — Claude 写代码，Codex 做 review，Cursor bugbot 做 triage
- **test-gated auto-revert** — 改完跑测试，挂了自动回退（不是"修好再测"，是直接回滚）
- **risk-tiered review** — 低风险改动轻审、高风险改动多重审
- **sameness detector** — 重试循环中检测是否原地打转，防止死循环
- **多模型编排** — 一个 YAML 文件定义每个角色用什么模型

### 3️⃣ vibecode-pro-max-kit（withkynam）
https://github.com/withkynam/vibecode-pro-max-kit

- **spec-driven**：按 spec 写代码
- **Tester 自动映射**：改的文件 → 对应测试文件，覆盖率 > 70% 自动跑全量
- **代码 Reviewer 独立**

### 4️⃣ best-of-Agent-Harnesses（RyanAlberts）
https://github.com/RyanAlberts/best-of-Agent-Harnesses

不是一个项目，是一个**100+ agent harness 的排行榜**，按评分排列。可以作为持续参考源。

---

## 二、核心流程设计

```
[灵霄接到 bug]
     ↓
[1. 影响域分析] — 这次改动会影响哪些文件/模块/测试？
     ↓
[2. 建立基线] — 跑相关测试，记录通过数/失败数/覆盖率
     ↓
[3. 修 bug] — 由主力 agent 改代码（灵霄/Cline + DeepSeek）
     ↓
[4. 改后验证]
  ├── 跑同批测试 → 对比基线 → 有回归？
  │      ├── 是 → 直接回退（auto-revert），不修
  │      └── 否 → 继续
  ├── lint/type check
  ├── 安全扫描（硬编码密钥、注入等）
  └── review（另一个 agent，独立审查）
     ↓
[5. 通过 → 合并 | 失败 → 回退]
```

---

## 三、边缘条件分析（关键部分）

### 1. 影响域分析
**做什么：** 改一个函数前，知道谁调用了它、它调用了谁
**边缘条件：**
- 动态调用（反射、getattr）→ 静态分析抓不到 → 怎么兜底？
- 配置文件/环境变量依赖 → 改代码影响到配置加载路径？
- 数据库 migration → 改 model 层是否需要同步改 migration？
- 答案是：**做不到 100% 精确的影响域分析**。兜底策略是：如果项目有测试覆盖，跑全量；如果没测试，标记为"高风险变更"，要求人工确认

### 2. 基线建立
**做什么：** 改之前跑一遍测试，记录状态
**边缘条件：**
- 基线本身就是红的（测试本来就没全过）→ 不能简单对比"新增失败"，需要 diff 出"原本失败 vs 现在通过"的集合
- 测试有 flaky（间歇性失败）→ 基线应该跑 2-3 次取稳定结果？
- 测试耗时太长（全量跑 30 分钟）→ 改一行代码等半小时验证，灵霄会疯。需要增量策略：只跑影响域内的测试，除非改动涉及核心基础设施

### 3. 代码修改
**做什么：** agent 改代码
**边缘条件：**
- 一次改了多个文件 → 逐个文件验证还是合起来验证？
- 改代码过程中测试框架挂了（比如改了 CI 配置文件）→ 验证步骤本身不可信
- 改完后文件格式坏了（YAML 缩进、JSON 少逗号）→ 语法校验先通过才算"改完"
- 解决了 bug A 但引入了 bug B → 这恰恰是我们要防的。由改后验证来拦截

### 4. 改后验证
**做什么：** 跑测试 + lint + review，对比基线
**边缘条件：**
- **角色必须严格分离** — 写代码的 agent 不能 review 自己的代码。这是硬规矩
- 全量测试 vs 增量测试的分界线在哪？——建议：改动 ≤ 3 个文件跑影响域测试，> 3 个文件或改核心文件跑全量
- 什么算"验证通过"？——测试零新增失败 + lint 无新增 error + review 通过
- 什么算"验证失败"？——任何一项不通过即失败
- review 的意见如果只是风格问题（缩进、命名），算 blocking 还是 non-blocking？

### 5. 回退机制
**做什么：** 验证失败时回滚
**边缘条件：**
- 回退什么范围？——只回退本次改动，还是回退到上一个已知健康状态？
- 回退后通知谁？——通知灵霄 + 记录到 log
- 是否自动重试？——检测"是否在原地打转"（sameness detector）：如果 3 次回退都在同一个测试上挂，停止重试，标记为"需要人工介入"
- 部分改动成功部分失败？——git 没有部分 revert，要么全退要么不退

### 6. 循环防护
**做什么：** 防止 agent 陷入死循环
**边缘条件：**
- 修了 n 次还在同一个问题上 → 超过 3 次后停止
- sameness detector：检测每次修改的 diff 是否高度相似（> 80% 相似度）→ 说明在原地打转
- 触发上限后的行为：标记为"需要人工排查"，发通知给灵霄/子言，不继续自动重试

### 7. Harness 本身
**做什么：** 这套自动化流水线
**边缘条件：**
- harness 自己的代码谁来审？——由灵昭/大壮审时一次写对，后面只修 bug 不改逻辑
- harness 出 bug 怎么办？——最坏情况：跳过 harness，直接让灵霄改。harness 只是个辅助层，不能成为阻塞
- 配置错误（比如测试命令配错了）→ harness 跑错结论（明明没回归说回归了）→ 需要人工校验机制
- 性能开销 → 每次改代码多花 2-5 分钟做验证。灵霄会不会觉得烦？如果会，需要设计"信任模式"：低风险改动跳过全量验证

---

## 四、建议灵霄的 .clinerules（草稿）

```
## 质量门禁（强制）

### 修改前
1. 跑 `pytest [相关模块] -q --tb=short`，记录结果作为基线
2. 用 `git stash` 暂存未完成的修改（如果已有改动），确保基线反映的是当前已提交的状态

### 修改中
3. 一次只改一个逻辑点
4. 改完一个文件后先检查语法（python: `python -m py_compile`, yaml: `yaml lint` 等）

### 修改后
5. 跑和基线相同的测试命令，对比结果
6. 如果有新增失败 → 撤回本次修改，排查原因
7. 如果没有测试覆盖改动的影响域 → 在消息中明确标注"无测试覆盖，风险区域：xxx"
8. 不要自己审查自己的 diff，提交给 review agent

### 边缘场景
- 基线测试本身就红 → 在改前改后都注明"基线已存在 N 个失败"，只关注新增失败
- 测试命令不存在 → 标注"本项目无自动化测试，变更风险评估高"
- 修改涉及数据库 schema / migration / 配置文件格式 → 强制标记为高风险
```

---

## 五、待讨论的问题（给灵昭）

1. 这个 harness 是独立脚本（像 mailbus 那样放在 /mnt/e/ai_tools/），还是嵌入到灵霄的 Cline 配置里？
2. 灵霄用的 Cline + DeepSeek，Cline 有没有 hook/plugin 机制来拦截"改完后"事件触发验证？
3. 验证工具链用 pytest 还是其他？
4. 项目有没有现有的 CI（GitHub Actions / Jenkins）？还是我们要从零搭？
5. auto-revert 的触发条件：测试失败就回退，还是参考 lint/review 的结果？
6. harness 的"信任模式"：哪些项目/哪些改动类型可以跳过全量验证？
7. sameness detector 的相似度阈值怎么设？
8. 谁来写这个 harness？按子言的规矩——我不写代码——建议灵昭出方案，大力/大壮落地，灵曦安全审计
