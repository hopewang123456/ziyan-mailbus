# mailbus 消息处理标准化流程

> 所有 AI 团队成员必须遵守。不遵守=消息丢失=任务延误。

---

## 一、收到消息后的标准三步流程

### 第 1 步：写 ACK（2秒内）

收到消息后，在**自己的 inbox 目录**下写 ack.json：

```json
{"action":"ack","msg_id":"<消息ID>","agent":"<你的key>","timestamp":"<ISO时间>"}
```

你的 ack 路径：`/mnt/e/ai_tools/mail/store/inbox/<你的key>/ack.json`

> ⚠️ 只回文字不算已读。mailbus 以 ack.json 文件为确认依据。

### 第 2 步：执行任务

写 ack 后**必须立即开始执行**消息中的任务，不得只 ack 不执行。

执行的内容取决于消息类型：
| 消息类型 | 执行内容 |
|---------|---------|
| `notice` | 阅后即 ack，无需回信 |
| `task` | 执行具体任务（审查/编码/测试等）|
| `task_reply` | 执行任务 + 回复发件人 |
| `question` | 回复发件人 |
| `forward` | 转发给指定 agent |

### 第 3 步：回复发件人（执行完毕后）

**🔴 强制规则：任务完成后必须回信给发送方，不得遗漏。**

每个人必须回复发件人，告知任务完成状态。不回复 = 发件人不知道你做好了。

**违反后果：**
- 第1次违规 → mailbus 自动记录 + 口头提醒
- 第2次违规 → 升级通知给灵昭
- 第3次违规 → 拉入黄牌名单，任务自动降权

**回复格式**（通过 mailbus API）：

```bash
curl -X POST http://localhost:9812/api/send-msg \
  -H "Content-Type: application/json" \
  -d '{"from":"<你的key>","to":"<发件人key>","type":"reply","priority":"normal","content":"任务完成报告：做了什么、结果如何"}'
```

回复内容必须有实质：
- ✅ 「灵霄的 PR 已审查完毕，发现 3 个逻辑问题，已标注行号返回」
- ✅ 「mailbus bug #2 已修复，config.json 中 lingxiao 和 dali 的 launch URL 已从裸端口改为 /chat」
- ❌ 「收到，已完成」——没说做了什么
- ❌ 「任务完成回执」——逃兵模板

---

## 二、消息优先级处理规则

| 优先级 | 含义 | 处理时间要求 |
|--------|------|------------|
| `urgent` | 阻塞性任务，不处理则其他人无法进展 | 收到后15分钟内响应 |
| `normal` | 常规任务 | 收到后2小时内响应 |
| `low` | 非紧急，有空再处理 | 当天内 |

如果无法按时完成 `urgent` 任务，**必须在15分钟内回复发件人告知延迟原因和预计完成时间**。

---

## 三、三种场景的操作流

### 场景 A：灵霄/大力写代码

```
灵霄/大力提交代码
  ↓
灵霄/大力通知灵鉴审查（通过 mailbus）
  ↓
灵鉴收到 → ACK → 审查 → 回复灵霄
  ↓ 审查通过
灵鉴通知灵验测试
  ↓
灵验收到 → ACK → 测试 → 回复灵鉴
  ↓ 测试通过
灵鉴通知小七验收
  ↓
小七验收 → 通知子言上线
```

**谁通知谁？** 上一个环节完成的人，主动向下一环节发消息。不要等别人来问。

### 场景 B：灵昭出方案

```
灵昭出方案 → 通知灵霄/小七执行
  ↓
灵霄/小七收到 → ACK
  ↓ 执行完毕
灵霄/小七回复灵昭告知结果
  ↓
灵昭确认完成 → 通知子言
```

### 场景 C：子言直接发任务

```
子言发消息给任何人
  ↓
收到 → ACK → 执行
  ↓ 执行完毕
回复子言（告知做了什么、结果如何）
```

**给子言回复的特别要求：**
- 一次性讲清楚：做了什么、结果如何、还差什么
- 不让子言追问细节
- 语气和善积极

---

## 四、催办与超时

mailbus 会自动检测超时未完成的任务，发出催办通知。

| 催办次数 | 行为 |
|---------|------|
| 第1次催办 | 提醒尽快处理 |
| 第2次催办 | 升级通知给发件人 |
| 第3次催办 | 任务自动标记为 timeout |

**避免催办的方法：**
- 写 ACK 后立即开始执行
- 如果任务需要比预期更长的时间，提前回复发件人说明
- 每天检查自己 inbox 是否有未处理的消息

---

## 五、禁止行为

| ❌ 禁止 | 为什么 |
|--------|--------|
| 只 ACK 不执行 | 欺骗发件人，浪费信任 |
| 执行完不回复 | 发件人以为你没做，重复催办 |
| 回复「完成回执」无实质内容 | 等于没回 |
| 多项任务只回最新那条 | 必须逐条回复 |
| 不检查 inbox 等别人催 | 主动检查是基本纪律 |

---

## 六、inbox 检查纪律

**每天至少检查 3 次自己的 inbox**（早/中/晚），不要等 mailbus 推送。

如果你感觉很久没人找你了——**主动检查 inbox**，不要等子言或灵昭来敲你。

检查方法：

```bash
curl http://localhost:9812/api/inbox/<你的key>
```

---

## 七、快速参考

| 操作 | 命令/路径 |
|------|----------|
| 查看自己 inbox | `curl http://localhost:9812/api/inbox/<你的key>` |
| 写 ack | 写入 `store/inbox/<你的key>/ack.json` |
| 回复发件人 | `curl -X POST ... /api/send-msg`（见上文）|
| mailbus dashboard | http://localhost:9812 |
| 查看 team 组织图 | mailbus dashboard 首页 |
