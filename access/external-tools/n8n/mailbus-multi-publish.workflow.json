{
  "name": "mailbus-multi-publish",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "mailbus-multi-publish",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "a1b2c3d4-webhook",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "mailbus-multi-publish"
    },
    {
      "parameters": {
        "jsCode": "const body = $json.body || $json;\nreturn [{\n  json: {\n    ok: true,\n    content_id: body.content_id || body.task_id || 'unknown',\n    platforms: body.platforms || [],\n    published_at: new Date().toISOString(),\n    source: 'n8n-mailbus-multi-publish',\n    note: 'stub — replace with real platform nodes'\n  }\n}];"
      },
      "id": "b2c3d4e5-prepare",
      "name": "Prepare Publish",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [480, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [[{ "node": "Prepare Publish", "type": "main", "index": 0 }]]
    }
  },
  "active": true,
  "settings": { "executionOrder": "v1" },
  "meta": {
    "description": "mailbus video_publish publish_exec → webhook-multi-publish"
  }
}
